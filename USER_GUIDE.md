# 企业微信智能机器人 - 用户使用指南

## 功能概览

本机器人是企业微信智能助手，支持以下功能：

- **对话交互**：与 AI 进行自然语言对话
- **文件处理**：上传图片和文档，自动解密并分析
- **文件引用**：引用历史文件进行进一步操作
- **命令执行**：通过自然语言或直接命令执行系统操作
- **会话管理**：独立的对话记录，支持清理
- **企业共享资料库**：发布和查询企业共享文件

---

## 基础使用

### 发送消息

直接发送文本消息与 AI 对话：
```
你好，请介绍一下你自己
```

### 上传文件

支持上传以下类型：
- **图片**：JPG、PNG、GIF、WEBP 等
- **文档**：PDF、Word、Excel 等

文件会自动：
1. 下载到服务器
2. 自动解密（企业微信加密格式）
3. 保存到用户独立目录

上传后的随机回复示例：
- "我收到了图片，请告诉我你需要什么帮助？"
- "文件收到啦，需要我帮你做什么？"

### 查看历史

机器人会记住对话历史，支持上下文对话。

---

## 高级功能

### 清理对话

清理所有对话记录、文件和会话历史：

```
用户：清理
机器人：确认要清理所有对话记录吗？此操作不可恢复。
      请回复 'yes' 确认，或回复 'cancel' 取消。
用户：yes
机器人：您的对话记录已清理完成。
```

**注意**：
- 清理后，引用历史文件会被拒绝（返回"您引用的消息不存在，请重新发送"）
- 确认有效期为 5 分钟

### 执行命令

支持两种方式执行系统命令：

#### 方式1：直接输入命令
```
用户：apt-get install vim
机器人：检测到需要管理员权限的命令：
      apt-get install vim
      
      请回复 'yes' 确认执行，或回复 'cancel' 取消。
用户：yes
机器人：命令执行完成。
      输出：...
```

#### 方式2：自然语言
```
用户：帮我安装 vim
机器人：检测到需要执行：
      命令：`sudo apt-get install vim`
      功能：安装 vim 软件包
      
      请回复 'yes' 确认执行，或回复 'cancel' 取消。
用户：yes
机器人：命令执行完成。
```

**允许的命令**：
- `apt-get`、`apt`：软件包管理
- `systemctl`：服务管理

**注意**：
- 所有需要 sudo 的命令都需要确认
- 确认有效期为 5 分钟
- 不在白名单的命令会被拒绝

### 引用文件

引用之前上传的图片或文件进行操作：

```
用户：[引用一张图片] 这张图里有什么？
机器人：这是一张抖音视频截图，展示了一个观点...
```

**说明**：
- AI 会自动附加引用文件的路径
- 引用已清理的文件会被拒绝
- 支持引用图片和文档

---

## 安全限制

### 文件限制

- **文件大小**：最大 100MB
- **文件类型**：支持常见图片和文档格式

### 命令限制

- **Sudo 白名单**：只允许执行 `apt-get`、`apt`、`systemctl`
- **命令确认**：所有需要 sudo 的命令都需要用户确认
- **自动执行**：不需要 sudo 的命令会自动执行

### 消息限制

- **消息去重**：60 秒内相同消息不会重复处理
- **确认超时**：清理和命令确认的 5 分钟有效期

---

## 企业共享资料库

### 概述

企业共享资料库允许用户将个人文件发布到企业共享区，供所有用户访问和使用。

### 发布文件

将个人文件发布到共享资料库：

#### 方式1：上传后直接发布
```
用户：[上传文件 report.pdf]
机器人：已收到文件：report.pdf
       请告诉我您希望如何处理这个文件？
用户：发布
机器人：确认要将文件「report.pdf」发布到企业共享资料库吗？
       请回复：确认 或 yes（其他任何回复都将自动取消）
用户：确认
机器人：文件「report.pdf」已成功发布到企业共享资料库！
```

#### 方式2：引用历史文件发布
```
用户：[引用一条历史图片消息] 发布
机器人：确认要将文件「photo.jpg」发布到企业共享资料库吗？
       请回复：确认 或 yes（其他任何回复都将自动取消）
用户：确认
机器人：文件「photo.jpg」已成功发布到企业共享资料库！
```

#### 方式3：带描述发布
```
用户：[引用文件] 发布这个文件 描述：2024年度总结报告
机器人：确认要将文件「report.pdf」发布到企业共享资料库吗？
       请回复：确认 或 yes（其他任何回复都将自动取消）
用户：确认
机器人：文件「report.pdf」已成功发布到企业共享资料库！
```

### 查询共享文件

查看企业共享资料库中的所有文件：

```
用户：有哪些文件
机器人：企业共享资料库文件列表：

       📷 图片：
       1. product.jpg
          描述: 产品展示图
          发布者: XuLi
          发布时间: 2026-02-12 10:00:00

       📄 文档：
       1. report.pdf
          描述: 2024年度总结
          发布者: XuLi
          发布时间: 2026-02-12 09:00:00
```

**支持的查询命令**：
- "共享文件" / "共享资料"
- "企业文件" / "企业资料"
- "有哪些文件" / "你有哪些文件"
- "有哪些图片" / "图片列表"
- "文件列表"

### 文件类型区分

系统自动将文件分为两类：
- **📷 图片**：JPG、JPEG、PNG、GIF、WEBP、BMP
- **📄 文档**：其他所有类型（PDF、Word、Excel 等）

### 发布确认机制

- 确认有效期：5 分钟
- 超时或发送其他消息会自动取消
- 支持"确认"、"yes"确认，"取消"、"cancel"取消

### 权限说明

- 所有用户都可以发布文件
- 所有用户都可以查询共享文件
- AI 可以访问共享文件进行分析和处理

---

## 自动清理策略

为节省磁盘空间，系统会自动清理旧文件：

| 文件类型 | 保留时间 | 清理方式 |
|---------|---------|---------|
| 未解密文件 | 7 天 | 自动删除 |
| 解密文件 | 1 年 | 自动删除 |

清理时机：每次启动服务时自动执行。

---

## 数据存储

### 文件存储位置

```
user_data/
└── {用户ID}/
    ├── workspace/      # iFlow 工作区
    └── files/          # 用户文件
        ├── {hash}.jpg          # 加密文件
        └── {hash}_decrypted.jpg # 解密文件

shared_files/           # 企业共享资料库
    └── {hash}.jpg      # 发布的共享文件
```

### 数据库

- 文件名：`wecom_bot.db`
- 记录：对话消息、文件映射、会话信息

---

## 常见问题

### Q: 上传文件后机器人不回复？

A: 可能原因：
1. 文件过大（超过 100MB）
2. 网络问题导致下载失败
3. 文件解密失败

### Q: 引用历史文件提示"消息不存在"？

A: 原因：
1. 已经清理了对话记录
2. 文件已过期被自动删除
3. media_id 验证失败

解决方案：重新上传文件

### Q: 执行命令提示"不在允许的命令列表中"？

A: 只允许执行 `apt-get`、`apt`、`systemctl` 命令。如需其他权限，请联系管理员。

### Q: 清理对话后文件还在吗？

A: 清理会删除：
- 对话记录
- 数据库中的文件映射
- 用户目录中的所有文件

### Q: 如何查看我的文件？

A: 目前需要通过管理员访问服务器查看：
```
user_data/{用户ID}/files/
```

### Q: 发布文件到共享资料库后，原文件还在吗？

A: 是的，发布操作会复制文件到共享目录，原文件仍然保留在您的个人目录中。

### Q: 可以删除已发布的共享文件吗？

A: 目前暂不支持通过机器人界面删除共享文件，需要联系管理员手动删除。

### Q: 共享文件会自动清理吗？

A: 不会。共享文件由管理员手动管理，不会自动删除。

### Q: 发布的文件可以编辑吗？

A: 不能。文件发布后是只读的，如需更新，可以重新发布新版本。

---

## 技术支持

如有问题，请联系管理员。

---

**版本**：1.1  
**更新日期**：2026-02-12