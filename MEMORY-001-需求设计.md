# MEMORY-001 - 需求设计

## 索引信息
- 索引号：M001
- 日期：2026-02-10
- 类型：需求设计

---

## 一、项目概述
**项目名称：** 企业微信智能机器人

**项目目标：** 将企业微信智能机器人作为 iFlow CLI 的消息代理，实现双向消息转发

---

## 二、核心需求

### 2.1 功能需求
企业微信智能机器人 ↔ iFlow CLI 双向消息转发

```
用户(企业微信) → 智能机器人 → iFlow CLI (通过 ACP 协议)
用户(企业微信) ← 智能机器人 ← iFlow CLI (通过 ACP 协议)
```

### 2.2 具体流程
1. 用户在企业微信中向智能机器人发送消息
2. 智能机器人接收消息，转发给终端中的 iFlow CLI
3. iFlow CLI 处理消息并生成回复
4. 智能机器人接收 iFlow CLI 的回复，发送给用户

---

## 三、技术方案

### 3.1 iFlow CLI 交互方式
- **启动 ACP 模式：** `iflow --experimental-acp --port 8090`
- **通信方式：** WebSocket 双向通信
- **协议：** ACP (Agent Communication Protocol)
- **SDK：** Python SDK (`iflow-cli-sdk`)

### 3.2 消息流转
```
企业微信 → 回调 URL (智能机器人服务)
           ↓
        消息解密
           ↓
    Python SDK (IFlowClient)
           ↓
    WebSocket (ACP 协议)
           ↓
      iFlow CLI
           ↓
    处理结果返回
           ↓
    加密回复
           ↓
 POST response_url
           ↓
    企业微信用户
```

---

## 四、会话管理需求

### 4.1 记忆问题
- **问题：** 多用户同时使用，如何保持各自的对话记忆？
- **方案：** 每个用户独立的 iFlow 会话

### 4.2 会话隔离
| 方案 | 实现方式 |
|------|----------|
| 方案1（推荐） | 为每个 `userid` 创建独立的 IFlowClient 实例 |
| 方案2 | 使用 `userid_` 作为会话标签前缀 |
| 方案3 | 使用数据库存储 `userid → session_id` 映射 |

### 4.3 会话持久化
- **存储位置：** `~/.iflow/history/`
- **管理方式：** Git 版本控制
- **功能：**
  - 自动保存检查点
  - 支持会话恢复
  - 支持版本回滚

---

## 五、架构设计

```
┌─────────────────────────────────────────────────────────────┐
│                    企业微信智能机器人服务                     │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │ 用户 A      │  │ 用户 B      │  │ 用户 C      │         │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘         │
│         │                │                │                │
│         ▼                ▼                ▼                │
│  ┌──────────────────────────────────────────────────┐      │
│  │         会话管理器 (Session Manager)              │      │
│  │  - 为每个用户创建独立会话                          │      │
│  │  - 管理 userid → session_id 映射                  │      │
│  │  - 自动保存和恢复会话状态                          │      │
│  └──────────────────────────────────────────────────┘      │
│                           │                                │
│                           ▼                                │
│  ┌──────────────────────────────────────────────────┐      │
│  │          iFlow CLI SDK (Python)                   │      │
│  │  - 每个用户一个 IFlowClient 实例                  │      │
│  │  - 通过 ACP 协议与 iFlow CLI 通信                 │      │
│  └──────────────────────────────────────────────────┘      │
│                           │                                │
│                           ▼                                │
│  ┌──────────────────────────────────────────────────┐      │
│  │              iFlow CLI 进程                        │      │
│  │  --experimental-acp --port 8090                    │      │
│  └──────────────────────────────────────────────────┘      │
│                           │                                │
│                           ▼                                │
│  ┌──────────────────────────────────────────────────┐      │
│  │         会话存储 (~/.iflow/history/)               │      │
│  │  - 每个会话独立目录                                │      │
│  │  - Git 管理历史版本                                │      │
│  └──────────────────────────────────────────────────┘      │
└─────────────────────────────────────────────────────────────┘
```

---

## 六、开发环境
- **系统：** Linux (树莓派)
- **Python：** 3.11.2
- **iFlow CLI：** 已安装 (0.5.8)
- **依赖：**
  - `iflow-cli-sdk` (Python SDK)
  - Web 框架：Flask 或 FastAPI
  - 加解密库：企业微信官方库

---

## 七、关键注意事项
1. 企业微信消息需要使用官方加解密库解密
2. response_url 有效期 1 小时，仅能用一次
3. iFlow 进程由 SDK 自动管理
4. 端口冲突由 SDK 自动检测
5. 使用同一个 IFlowClient 实例保持会话
6. 需要设置合理的超时时间

---

## 八、记忆系统需求

### 8.1 需求背景
- 防止 AI 助手在开发过程中重复探索相同的问题
- 记录成功的尝试和失败的教训
- 便于快速查询已有的技术资料和决策

### 8.2 记忆系统设计
**文档结构：**
- `INDEX.md` - 索引文档，包含所有条目的快速查找表
- `MEMORY-001-需求设计.md` - 需求和架构设计
- `MEMORY-002-技术资料.md` - API 文档和技术方案
- `MEMORY-003-成功尝试.md` - 成功的方案和经验
- `MEMORY-004-失败尝试.md` - 失败的尝试和错误

**索引号规则：**
| 类型 | 前缀 | 说明 |
|------|------|------|
| 需求设计 | M001 | 项目需求、架构设计 |
| 技术资料 | M002 | API文档、技术方案、协议说明 |
| 成功尝试 | M003 | 成功实现的方案、可用的代码 |
| 失败尝试 | M004 | 失败的尝试、错误记录、避免的坑 |
| 对话记录 | M005 | 重要的对话内容、决策过程 |

**使用流程：**
1. 遇到问题时先查询 `INDEX.md`
2. 通过关键词或索引号找到对应条目
3. 在对应的详细文档中查看完整信息
4. 新内容需要更新索引和对应文档

### 8.3 维护规则
- 每次重要对话后更新索引
- 成功的尝试记录到 MEMORY-003
- 失败的尝试记录到 MEMORY-004
- 技术资料整理到 MEMORY-002

---

## 九、后续待确认
- [ ] 确认 Web 框架选择（Flask 或 FastAPI）
- [ ] 确认数据库选择（SQLite 或 Redis）
- [ ] 确认超时时间设置
- [ ] 确认是否需要用户认证