# RAG 知识库系统 - 技术方案

## 1. 方案概述

RAG（Retrieval-Augmented Generation，检索增强生成）是一种结合信息检索和生成式 AI 的技术方案。通过将本地技术资料向量化存储，用户可以通过企业微信以对话形式快速查询和定位相关内容。

### 1.1 核心价值

- **快速定位**：基于语义搜索，比关键词搜索更智能
- **上下文回答**：AI 基于真实文档内容回答，减少幻觉
- **持续学习**：新增文档只需重新向量化，无需重新训练
- **隐私安全**：数据完全本地化，不依赖外部云服务

### 1.2 应用场景

- 技术文档查询
- API 接口文档
- 项目规范和流程文档
- 个人知识库管理

## 2. 技术架构

```
┌─────────────────┐
│  本地技术资料    │
│  (PDF/Word/MD)  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   文档解析层     │
│  PyPDF2/pypandoc│
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   文本切分层     │
│  LangChain/Chunk│
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   向量化层       │
│  BGE/OpenAI Embedding
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  向量数据库      │
│  Chroma/Milvus  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  企业微信机器人  │
│  对话接口       │
└─────────────────┘
```

## 3. 软硬件条件

### 3.1 硬件要求

| 配置项 | 最低要求 | 推荐配置 | 说明 |
|--------|---------|---------|------|
| CPU | 2 核 | 4 核+ | 用于文档解析和向量计算 |
| 内存 | 4 GB | 8 GB+ | 向量数据库和 AI 模型需要内存 |
| 存储 | 10 GB | 50 GB+ | 存储文档和向量数据 |
| 网络 | - | - | 本地运行无需网络（除非使用云服务） |

**当前环境评估** (Raspberry Pi 5):
- CPU: 4 核 ✅
- 内存: 8 GB ✅
- 存储: 50 GB+ ✅
- **结论：满足要求**

### 3.2 软件要求

| 软件 | 版本 | 用途 |
|------|------|------|
| Python | 3.8+ | 主要开发语言 |
| SQLite | 3.x | 向量元数据存储 |
| ChromaDB | 最新版 | 轻量级向量数据库（推荐） |
| BGE Embedding | v1.5+ | 本地向量模型（中文优化） |
| PyPDF2 | 最新版 | PDF 文档解析 |
| python-docx | 最新版 | Word 文档解析 |
| LangChain | 最新版 | 文本处理框架 |

### 3.3 依赖包清单

```bash
# 向量数据库
pip install chromadb

# 文档解析
pip install PyPDF2 python-docx pypandoc

# 向量化模型
pip install sentence-transformers

# 文本处理
pip install langchain langchain-community

# 其他
pip install numpy tqdm
```

## 4. 技术栈选择

### 4.1 向量数据库对比

| 方案 | 优势 | 劣势 | 推荐度 |
|------|------|------|--------|
| ChromaDB | 轻量、易用、无需额外服务 | 大规模性能一般 | ⭐⭐⭐⭐⭐ |
| Milvus | 高性能、可扩展 | 部署复杂 | ⭐⭐⭐ |
| Pinecone | 云服务、简单 | 付费、数据出境 | ⭐⭐ |
| Weaviate | 功能丰富 | 资源占用大 | ⭐⭐⭐ |

**推荐：ChromaDB** - 单文件存储，无需额外部署，适合轻量级场景

### 4.2 向量模型对比

| 模型 | 优势 | 劣势 | 推荐度 |
|------|------|------|--------|
| BAAI/bge-small-zh-v1.5 | 中文优化、体积小、速度快 | 英文表现一般 | ⭐⭐⭐⭐⭐ |
| BAAI/bge-large-zh-v1.5 | 中文性能好 | 体积大、速度慢 | ⭐⭐⭐⭐ |
| OpenAI Embeddings | 性能最强 | 付费、网络依赖 | ⭐⭐⭐ |

**推荐：BAAI/bge-small-zh-v1.5** - 适合本地部署，性价比高

## 5. 实现步骤

### 5.1 阶段一：基础设施搭建

1. **安装依赖**
   ```bash
   pip install chromadb sentence-transformers PyPDF2 python-docx
   ```

2. **创建向量数据库管理模块**
   - `knowledge_base.py` - 向量数据库初始化和管理
   - 支持文档添加、删除、更新

3. **实现文档解析器**
   - 支持 PDF、Word、Markdown 格式
   - 统一文本提取接口

### 5.2 阶段二：向量化处理

1. **文本切分**
   - 按段落或固定长度切分
   - 保留上下文信息

2. **向量化存储**
   - 加载 BGE 模型
   - 批量处理文档
   - 存储向量 + 元数据

### 5.3 阶段三：检索与集成

1. **语义检索**
   - 问题向量化
   - 相似度搜索
   - 返回 Top-K 相关片段

2. **集成到企业微信机器人**
   - 在 `process_message_and_get_reply` 中添加检索逻辑
   - 构造带上下文的提示词
   - AI 基于检索结果回答

### 5.4 阶段四：功能增强

1. **增量更新**
   - 监控目录变化
   - 自动向量化新文档

2. **查询优化**
   - 多轮对话上下文
   - 结果重排序

3. **管理界面**
   - 文档管理命令
   - 统计信息展示

## 6. 与现有系统集成

### 6.1 集成点

```
app.py
  └─ process_message_and_get_reply()
       ├─ 知识库检索（新增）
       │  └─ knowledge_base.search(query)
       └─ AI 回复（增强）
          └─ prompt + 检索结果 → AI
```

### 6.2 提示词设计

```
你是一个知识库助手。请基于以下参考资料回答用户问题：

[参考资料]
{检索到的文档片段}

[用户问题]
{用户问题}

要求：
1. 仅基于参考资料回答，不要编造
2. 引用来源时标注文档名
3. 如果资料不足，请说明
```

### 6.3 对话命令

| 命令 | 功能 | 示例 |
|------|------|------|
| `添加知识库 /path/to/docs` | 添加文档目录 | `添加知识库 /home/pi/docs` |
| `查询 [问题]` | 查询知识库 | `查询 Python 如何处理 PDF` |
| `知识库状态` | 查看统计信息 | `知识库状态` |
| `重建索引` | 重新向量化 | `重建索引` |

## 7. 性能优化

### 7.1 存储优化

- 压缩向量数据
- 定期清理无效文档
- 使用索引加速查询

### 7.2 检索优化

- 使用 HNSW 索引
- 批量查询
- 缓存热门查询

### 7.3 模型优化

- 量化模型（INT8）
- 模型蒸馏
- GPU 加速（如可用）

## 8. 安全与隐私

### 8.1 数据安全

- 所有数据本地存储
- 不依赖外部 API
- 可选加密存储

### 8.2 访问控制

- 用户级别隔离
- 敏感文档过滤
- 操作日志记录

## 9. 预期效果

### 9.1 性能指标

| 指标 | 目标值 |
|------|--------|
| 文档向量化速度 | 100 文档/分钟 |
| 检索响应时间 | < 1 秒 |
| 回答准确率 | > 85% |
| 支持文档数量 | 10,000+ |

### 9.2 用户体验

```
用户: Python 如何处理 PDF 文件？

机器人: 根据知识库文档，处理 PDF 有以下方法：

1. 使用 PyPDF2 库：
   from PyPDF2 import PdfReader
   reader = PdfReader("file.pdf")

2. 使用 pdfplumber（表格友好）：
   import pdfplumber
   with pdfplumber.open("file.pdf") as pdf:
       page = pdf.pages[0]

详细代码示例请参考《Python 文档处理指南.pdf》第 3 章。
```

## 10. 扩展计划

### 10.1 短期目标

- [x] 基础框架搭建
- [ ] 支持 PDF/Word 文档
- [ ] 语义检索功能
- [ ] 企业微信集成

### 10.2 中期目标

- [ ] 多模态支持（图片）
- [ ] 多语言支持
- [ ] 知识图谱构建

### 10.3 长期目标

- [ ] 智能问答升级
- [ ] 知识挖掘与推荐
- [ ] 多用户协作

---

**文档版本**: 1.0
**创建日期**: 2026-02-11
**维护者**: iFlow CLI